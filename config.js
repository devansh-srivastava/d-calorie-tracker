// Local deployment configuration. Add the Apps Script /exec URL and secret here
// for a live sheet connection; this file is intentionally ignored by Git.
window.APEX_CONFIG = { API_URL: '', API_SECRET: '' };
